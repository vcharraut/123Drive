[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "123Drive"
version = "0.3.2"
description = "123D arrow files -> PufferDrive binaries"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Valentin Charraut" },
]
keywords = ["autonomous-driving", "dataset-conversion", "pufferdrive", "123d"]
classifiers = [
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Operating System :: OS Independent",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "numpy",
  "tqdm",
  "joblib",
  "scipy",
  "shapely",
  "py123d>=0.6.0,<0.8",
]

[project.optional-dependencies]
viz = [
  "fastapi",
  "uvicorn[standard]",
]
dev = [
  "pytest",
  "ruff",
  "ty==0.0.48",
]
docs = [
  "furo",
  "sphinx",
]
all = [
  "123Drive[viz,dev,docs]",
]

[project.scripts]
convert = "bin_factory.main:main"
mapforge = "mapforge.affine:main"
build = "docker_tools.cli:main"
web = "viz.server:main"

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["bin_factory", "bin_factory.*", "mapforge", "mapforge.*", "viz", "viz.*", "docker_tools", "docker_tools.*"]

[tool.setuptools.package-data]
bin_factory = ["presets.toml"]
viz = ["web/static/*"]
docker_tools = ["dockerfiles/*", "*.py"]

[project.urls]
Homepage = "https://github.com/vcharraut/123Drive"
Documentation = "https://vcharraut.github.io/123Drive/"
Repository = "https://github.com/vcharraut/123Drive"
Issues = "https://github.com/vcharraut/123Drive/issues"

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = [
    "F",    # Pyflakes
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "C90",  # mccabe
    "I",    # isort
    "N",    # pep8-naming
    # "D",    # pydocstyle
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "A",    # flake8-builtins
    "C4",   # flake8-comprehensions
    "ISC",  # flake8-implicit-str-concat
    "ICN",  # flake8-import-conventions
    "PIE",  # flake8-pie
    "PT",   # flake8-pytest-style
    "PYI",  # flake8-pyi
    "RET",  # flake8-return
    "SIM",  # flake8-simplify
    "TID",  # flake8-tidy-imports
    "TCH",  # flake8-type-checking
    "ARG",  # flake8-unused-arguments
    "ANN",  # flake8-annotations
    "PTH",  # flake8-use-pathlib
    "ERA",  # eradicate
    "NPY",  # numpy
    "RUF",  # ruff-specific rules
]

ignore = [
    "ANN401",  # Any is allowed at the py123d / numpy boundary
    "ISC001",  # Conflicts with the Ruff formatter
    "D100",    # Missing docstring in public module
    "D104",    # Missing docstring in public package
]

[tool.ruff.lint.per-file-ignores]
"src/bin_factory/transforms/pipeline.py" = ["ARG001"]  # stages share a uniform (scenario, extras, config) signature
"tests/**" = ["ARG", "A002", "ANN"]  # test helpers take unused stub args, shadow builtins (id, exit), and skip annotations

[tool.ruff.lint.isort]
lines-after-imports = 2

[tool.ruff.lint.mccabe]
max-complexity = 20

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ty.environment]
python-version = "3.13"
extra-paths = ["src"]
